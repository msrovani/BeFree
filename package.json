{
  "name": "befree-os",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "sdk/*",
    "contracts",
    "packages/*",
    "infra/*"
  ],
  "scripts": {
    "build": "echo build monorepo",
    "dev": "echo start dev env",
    "test": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
