{
  "name": "befree-os",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "sdk/*",
    "contracts",
    "packages/*",
    "infra/*"
  ],
  "scripts": {
    "build": "echo build monorepo",
    "dev": "echo start dev env",
    "test": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
